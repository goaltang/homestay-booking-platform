# 任务：homestay3 系统全面探索与文档差距审计

> 你是探索型 agent，任务是**只读盘点**整个仓库并产出结构化报告。禁止修改任何源码/文档/配置（唯一的写操作是把报告写到指定路径）。禁止运行构建、测试、服务启动。

## 工作目录与环境事实

- 仓库：/mnt/d/homestay3（Windows WSL 挂载盘，读写正常）
- 结构：homestay-front/（用户端+房东端，Vue3+TS+Vite）、homestay-admin/（管理后台，同栈）、homestay-backend/（Spring Boot 3.0.2 + Java 17 + JPA + Flyway + Security/JWT + Redis + ES + RabbitMQ）、obsidian-vault/（项目文档，Obsidian 格式）、docs/（旧文档）、tasks/（任务书归档）
- GitHub 公开仓库：goaltang/homestay-booking-platform（已有 README 中文 + README_EN 英文）
- 近期（2026-08）新增能力，README 可能未覆盖：接口限流注解、LLM 调用重试、AOP 操作日志、springdoc 接口文档、首页统计五路 count 并行化（CompletableFuture）、管理后台构建分包优化与仪表盘真实环比

## 交付物

把报告写入 **/mnt/d/homestay3/tasks/2026-08-12-系统探索报告.md**（UTF-8，中文）。报告结构如下，每节必须给出**具体证据（类名/路径/数量/接口路径），禁止泛泛而谈**：

### 1. 仓库总览
- git 统计：提交总数（`git rev-list --count HEAD`）、最新 5 条提交、`git log --stat` 规模印象
- 代码量：各子项目文件数与大致行数（可用 `find ... | wc -l`，不必精确到行）
- 顶层目录/文件清单及职责（README.md、docs/、tasks/、tools/、docker-compose.yml 等）

### 2. 后端盘点（homestay-backend）
- **Controller 完整清单**：按包/类列出每个 Controller 的类名、路由前缀（@RequestMapping）、接口数量、代表性端点（每个类列 2-3 个即可）。全部列完，不要遗漏
- **Service 清单**：service/ 与 service/impl/ 下的类名，按业务模块归类（搜索/推荐/支付/订单/营销/定价/AI客服等）
- **Entity 清单**：entity/ 下实体类名列表（数量 + 名字即可）
- **Job 定时任务**：job/ 下任务类名 + @Scheduled 说明（周期、作用）
- **横切能力落地证据**（每项给出类名+位置）：
  - 接口限流注解（如 @RateLimit 之类）与切面实现、SpEL 用法示例
  - AOP 操作日志注解（@OperationLog 之类）与切面
  - LLM 调用重试（LlmClient 或相关类中重试机制）
  - springdoc：依赖坐标（pom.xml）、配置项、Swagger UI 访问路径、是否启用
  - 首页统计并行化：哪个 Service/Controller 用了 CompletableFuture，几路并行
  - Redis 分布式锁工具类
- **MQ 三场景**：每个场景的 Producer/Consumer 类名、交换机/队列/routing key 名、降级开关配置键（如 `order.timeout.mq-enabled`）
- **配置盘点**：application.properties 全部配置键按前缀分组列出（spring.*、jwt.*、payment.*、file.*、agent.*、*.mq-enabled 等），标注哪些是 2026-08 新增
- **Flyway**：db/migration/ 下 V 开头迁移文件数量、最新版本号（V1~Vxx 实际到几）
- **测试结构**：src/test/java 目录树 + 测试类清单 + 每个测试类大致断言点（1 句）

### 3. 前端盘点
- **homestay-front**：src 目录树（views/components/api/stores/router 等）、路由总数、views 页面清单、api 封装文件清单、pinia store 清单、package.json 主要依赖与版本
- **homestay-admin**：同上结构
- 两个前端的 build 配置要点（vite.config 代理目标、分包/懒加载优化痕迹）

### 4. 文档差距审计（本任务核心，花最多精力）
对照以下材料与代码实际：
- README.md（项目亮点、功能概览三端表、后端能力表、技术栈表、配置表、测试说明）
- README_EN.md（同上，英文版）
- obsidian-vault/03-后端/后端-Controller 接口清单.md
- obsidian-vault/00-索引/Homestay 项目索引.md

找出并列出（**每条给出证据：文档哪一行/哪一节 vs 代码哪个文件**）：
- **A. 缺失**：代码里已有、但 README/vault 完全没提的能力（尤其 2026-08 新增：限流、LLM 重试、springdoc、首页统计并行化、仪表盘真实环比、构建分包优化、AOP 操作日志）
- **B. 过时**：文档写了但已变化的内容（接口清单与 controller 实际不符的条目、已改名/已删除的模块、版本号错误、Flyway 迁移数错误）
- **C. 错误**：文档与代码明显矛盾的地方

### 5. 结论与建议
- TOP 10 差距清单：按对读者（面试官/新贡献者）的重要性排序，每条格式：`[缺失|过时|错误] 位置 -> 问题 -> 建议动作`
- 你对 README/vault 结构优化的建议（哪些该合并、哪些该补）

## 执行要求

- 先快速通读关键文件建立全局观，再逐模块深入；优先用 grep/find 定位，不要通读大文件
- 报告**必须写盘成功**（写完用 `wc -l` 确认文件存在且非空）
- 全程只读，不改任何其他文件
- 报告保持客观，不确定的地方标注"待确认"
