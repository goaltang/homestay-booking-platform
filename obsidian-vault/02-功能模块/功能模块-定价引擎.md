---
title: 定价引擎
date: 2026-06-05
tags:
  - homestay
  - feature
---

# 定价引擎

> 提供房源动态报价计算及多维度价格策略管理。路由：`/api/pricing/*`, `/api/admin/pricing-rules/*`, `/api/host/pricing-rules/*` | 权限：`ADMIN`, `HOST`, `LANDLORD`, `USER`

## 一、功能清单

### 1.1 用户可见功能

| 功能 | 说明 |
|------|------|
| 查看实时计价明细 | C端用户在房源预订或确认订单时，可查看每日房费、周末溢价、节假日溢价、早鸟与连住优惠的具体扣减详情 |
| 管理个人定价规则 | 房东（HOST/LANDLORD）可查看、创建、修改及删除个人拥有的房源定价规则（仅限于 HOST、HOMESTAY 或 GROUP 作用域） |
| 管理全局定价规则 | 系统管理员（ADMIN）可查看、创建、修改及删除系统内所有规则，包括全局周末、节假日定价与早鸟、连住优惠等 |

### 1.2 管理/系统功能

| 功能 | 说明 |
|------|------|
| 定价动态计算引擎 | 在用户发起询价或下单时，实时拉取生效规则并以特定优先级级联计算出最终应付金额 |
| 节假日自动同步预置 | 系统内置 2026 年法定节假日及调休数据，替代传统的硬编码逻辑 |
| 节假日覆盖率定时监控 | 后台监控任务定期校验未来节假日数据完整性，预防因数据缺失造成的溢价计算失败 |

---

## 二、核心业务规则

### 2.1 规则优先级与叠加计算逻辑

1. **优先级顺序**（由低到高，`priority` 字段升序应用）：
   * `WEEKEND` (周末溢价)
   * `HOLIDAY` (节假日溢价)
   * `EARLY_BIRD` (早鸟折扣)
   * `LONG_STAY` (连住特惠)
2. **叠加与截断机制**：
   * 所有规则按照 `priority` 升序链式计算。
   * 若某条规则的 `stackable` (是否可叠加) 字段为 `false`，则在应用该规则后中断后续所有计价规则的计算。
3. **折扣换算说明**：
   * 采用 `DISCOUNT_RATE` 时，传入的值表示折扣比例（例如：`0.10` 代表扣减 10% 的金额，即打 9 折；`0.15` 代表扣减 15% 的金额，即打 85 折）。

### 2.2 角色权限与所有权规则

| 角色 | 权限范围 | 所有权限制 |
|------|----------|------------|
| **ADMIN** | 可操作所有作用域的规则（`GLOBAL`, `HOST`, `HOMESTAY`, `GROUP`） | **无限制**：由于无超级管理员之分，任意管理员可修改或删除包含系统预置、其他管理员创建的所有规则 |
| **HOST / LANDLORD** | 仅能创建或管理作用域为 `HOST`, `HOMESTAY`, `GROUP` 的规则 | **严格所有权**：仅能修改或删除其自身创建的规则，不能跨房东修改 |

---

## 三、页面与组件

### 3.1 路由

| 路径 | 组件 | 说明 |
|------|------|------|
| `/order/confirm` | `OrderConfirm.vue` | 订单确认页面（集成价格细分折叠面板） |
| 后台管理路径 | `PricingRuleManage.vue` | 定价规则管理列表与编辑表单 |

### 3.2 组件结构

```text
OrderConfirm.vue (订单确认主页)
└─ Price Breakdown Panel (价格折叠明细面板 - 新增)
   ├─ Daily Rate List (每日单价及溢价原因展示)
   └─ Discount Detail List (各项折扣金额及优惠说明展示)
```

### 3.3 状态管理

* 采用 **Caffeine** 本地高性能内存缓存维护计价规则状态（缓存区名称 `pricingRules`）。
* 在执行规则的更新、修改、删除和状态切换操作时，自动触发 Spring 缓存驱逐（`@CacheEvict(allEntries = true)`），保持强一致性。

---

## 四、后端接口

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/api/pricing/quote` | 获取房源在特定入住时段的实时报价与明细 |
| GET | `/api/admin/pricing-rules` | 管理员分页查询所有价格规则 |
| POST | `/api/admin/pricing-rules` | 管理员创建新价格规则 |
| PUT | `/api/admin/pricing-rules/{id}` | 管理员修改指定价格规则 |
| DELETE | `/api/admin/pricing-rules/{id}` | 管理员删除指定价格规则 |
| PATCH | `/api/admin/pricing-rules/{id}/toggle` | 管理员切换规则启用状态 |
| GET | `/api/host/pricing-rules` | 房东查询个人拥有的价格规则列表 |
| POST | `/api/host/pricing-rules` | 房东创建价格规则 |
| PUT | `/api/host/pricing-rules/{id}` | 房东修改个人价格规则 |
| DELETE | `/api/host/pricing-rules/{id}` | 房东删除个人价格规则 |

---

## 五、数据模型

### 5.1 数据库表

#### 1. 价格规则表 (`pricing_rule`)

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | bigint (PK) | 规则自增唯一标识 |
| `name` | varchar(100) | 规则名称 |
| `scope_type` | varchar(20) | 作用域：`GLOBAL`, `HOST`, `HOMESTAY`, `GROUP` |
| `scope_value_json` | text | 作用域值，例如 `{ "homestayId": 1 }` 或 `{ "hostId": 2 }` |
| `rule_type` | varchar(20) | 规则类型：`WEEKEND`, `HOLIDAY`, `EARLY_BIRD`, `LONG_STAY` |
| `adjustment_type` | varchar(20) | 调整类型：`MULTIPLY`, `DISCOUNT_RATE` 等 |
| `adjustment_value` | decimal(10,2) | 调整系数值 |
| `priority` | int | 计算优先级（数值越小优先级越低，越先计算） |
| `stackable` | boolean | 是否允许与其它规则叠加 |
| `start_date` | date | 规则生效开始日期 |
| `end_date` | date | 规则生效结束日期 |
| `min_nights` | int | 连住天数阈值 |
| `min_advance_days` | int | 早鸟提前天数阈值 |
| `enabled` | boolean | 是否启用 |
| `created_by` | bigint | 创建人用户 ID |

#### 2. 节假日日历表 (`holiday_calendar`)

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | bigint (PK) | 主键 |
| `date` | date | 具体日期 |
| `name` | varchar(50) | 节日/调休名称 |
| `type` | varchar(30) | 类型：`PUBLIC_HOLIDAY`, `MAKEUP_WORKDAY` |
| `region_code` | varchar(10) | 地区代码（默认 CN） |
| `is_holiday` | boolean | 是否放假 |
| `is_makeup_workday` | boolean | 是否补班调休 |

---

## 六、实时同步 / 特殊机制

1. **本地多级缓存退避**：
   * 鉴于原有分布式 Redis 缓存连接不稳定带来的响应延迟，本模块重构为使用 **Caffeine 本地高性能缓存**。
   * 配置缓存区 `pricingRules`，设置 TTL 限制为 5 分钟，最大内存承载容量为 10 条。
2. **节假日监控定时任务**：
   * 采用后台定时线程，每日自动扫描节假日数据。
   * 当未来 30 天内包含公共法定节假日但日历表中无配置记录时，触发预警，避免计算周末及特殊价格异常。

---

## 七、实现记录

| 日期 | 功能 | 说明 |
|------|------|------|
| 2026-06-05 | 核心定价引擎服务重构 | 提取 Controller 中的逻辑并沉淀到 `AdminPricingRuleService` 和 `HostPricingRuleService`；修正 Admin 权限下的所有权校验，支持普通管理员全面编辑全局与预置规则 |
| 2026-06-05 | 2026年节假日补充与折扣定义修正 | 补充 2026 年元旦、春节、国庆等完整调休和补班数据；修正早鸟与连住的 `DISCOUNT_RATE` 的值表示形式为折扣扣减比例 |
| 2026-06-05 | 缓存策略迁移 | 移除 Redis 复杂缓存，迁移至高性能本地 Caffeine 缓存，并加入 `pricingRules` 缓存自动失效机制 |
| 2026-06-05 | 价格构成明细可视化 | 前端 `OrderConfirm.vue` 引入折叠明细面板，清晰呈现基础价、周末溢价、节假日溢价、早鸟与连住扣减过程 |

**验证结果**
- 后端编译/测试：✅ (已在 test profile 下通过测试校验)
- 前端构建：✅ (本地及 Admin 打包正常)

---

## 八、简历项目介绍与面试话术复习库

> [!TIP]
> 当你需要向招聘方或外部审计人员介绍本项目或此模块时，可以使用以下黄金话术：

### 1. 简历话术
* **核心职责**：主导民宿动态定价引擎系统的开发与调优，支撑“一房一价”精细化运营。
* **业务实现**：设计了链式优先级定价器，通过自定义的 priority 和 stackable 策略实现周末价、法定调休节假日价格浮动以及早鸟、连住折扣在支付网关前的混合计算。
* **架构演进**：将臃肿的 Controller 控制层逻辑沉淀为宿主隔离的专属 Service。针对规则的高频读取，将依赖网络IO的 Redis 移除，平滑迁移为基于本地 JVM 的 Caffeine 缓存（5min TTL + 声明式驱逐），消除外部网络抖动风险，提升读取性能。
* **用户体验**：重构预订结算页面，支持详细计价明细（Breakdown）展示，显著降低用户计价投诉率。

### 2. 面试 STAR 问答要点
* **S (情境)**：民宿预订中价格随日期、提前天数、入住时长动态高频变化，旧的定价代码缺乏多维折扣的合理叠加算法，且数据库面临并发查询压力。
* **T (任务)**：重构定价算法链，确保计算逻辑严密，并针对各种管理角色（Host, Admin）进行安全与数据隔离，且必须实现高性能数据读取。
* **A (行动)**：重新设计规则引擎优先级与叠加算法；编写独立的 Admin/Host 服务层；将底层缓存升级为 Caffeine 并实现双写缓存失效机制；前端实现详细账单折叠明细。
* **R (结果)**：缓存命中率提升，数据库读QPS显著降低；账单明细化后用户投诉率降低；非硬编码的节假日监控实现了自动化运维。

---

## 相关模块

- [[功能模块-订单管理]]
- [[功能模块-营销促销]]
- [[功能模块-支付与财务]]
