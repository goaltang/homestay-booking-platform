---
title: 通知与仪表盘
date: 2026-04-27
tags:
  - homestay
  - feature
---

# 通知与仪表盘

> Admin 端系统首页数据概览与通知入口。路由：`/dashboard`、`/notifications` | 权限：管理员

## 一、功能清单

### 1.1 系统首页（Dashboard）

| 功能 | 说明 |
|------|------|
| 查看关键指标 | 今日订单数、今日收入、待审核数、新用户数 |
| 查看快捷操作 | 常用功能入口快速跳转 |
| 查看最近活动 | 平台近期关键事件流 |
| 查看图表概览 | 订单/收入趋势图表 |

> **2026-08-12 更新（审查修复）**
> - 指标卡片趋势改为**真实环比昨日**（后端 `/api/admin/statistics` 新增 `yesterday` 字段；昨日为 0 时不显示趋势，**移除原随机数伪造趋势**）
> - 图表加载增加 v-loading；echarts 由全量引入改为按需（core + Line/Pie + 组件 + CanvasRenderer，`zrender` LinearGradient）
> - 饼图色板 8 色扩 12 色，空数据（无房源分布）显示占位项
> - resize 监听改命名函数 + 200ms 防抖，卸载时正确移除（修复监听器泄漏）

### 1.2 通知中心

Admin 端通知中心为管理员接收房源审核、系统公告等通知的入口。

| 功能 | 说明 |
|------|------|
| 查看通知列表 | 分页展示管理员收到的通知 |
| 筛选通知 | 按通知类型和已读状态筛选 |
| 标记已读 | 单条标记已读、全部标记已读 |
| 删除通知 | 删除单条通知 |
| 点击跳转 | 房源类通知跳转审核工作台/审核历史 |

> 通知中心完整架构见 [[功能模块-通知中心]]。

## 二、页面与组件

### 2.1 路由

| 路径 | 组件 | 说明 |
|------|------|------|
| `/dashboard` | `dashboard/index.vue` | 系统首页（Dashboard） |
| `/notifications` | `notifications/index.vue` | Admin 通知中心 |

### 2.2 Dashboard 组件结构

```
dashboard/index.vue
├── 指标卡片区域（今日订单/收入/待审核/新用户）
├── 快捷操作入口
├── 图表概览（订单趋势/收入趋势）
└── 最近活动流
```

### 2.3 通知中心组件结构

```
notifications/index.vue
├── page-header（标题 + 全部已读 + 刷新）
├── filter-section（类型筛选 + 状态筛选 + 搜索/重置）
└── notifications-list
    └── notification-item × N
        ├── 图标
        ├── 标题 + 时间 + 内容
        ├── entity meta（如房源ID tag）
        └── 标记已读 / 删除
    └── el-pagination
```

## 三、后端接口

### 3.1 Dashboard

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/admin/statistics` | 总览统计（total / period / today / **yesterday**） |
| GET | `/api/admin/statistics/order-trend` | 订单趋势（近 7/30 天） |
| GET | `/api/admin/statistics/homestay-distribution` | 房源省份分布 |
| GET | `/api/admin/statistics/user-trend` | 用户增长趋势 |

### 3.2 通知中心

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/notifications` | 获取通知列表（复用 C 端同一接口） |
| GET | `/api/notifications/unread-count` | 获取未读数量 |
| POST | `/api/notifications/{id}/read` | 标记已读 |
| POST | `/api/notifications/read-all` | 全部标记已读 |
| DELETE | `/api/notifications/{id}` | 删除通知 |

> 完整接口定义与 Service 层见 [[功能模块-通知中心]]。

## 四、实现记录

| 日期 | 功能 | 说明 |
|------|------|------|
| 2026-04-27 | Dashboard 页面 | 实现指标卡片、快捷操作、图表概览、活动流 |
| 2026-04-27 | Admin 通知中心 | 实现 `/notifications` 页面，支持筛选、分页、标记已读、删除 |

**验证结果**
- 前端构建：✅

## 相关模块

- [[功能模块-通知中心]] — 通知中心完整架构（数据模型、WebSocket、定时任务、Service 层）
- [[功能模块-统计分析]] — Dashboard 数据来源
- [[功能模块-订单管理]] — 订单通知
- [[功能模块-系统管理]] — 系统配置入口
